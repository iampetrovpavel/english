const learned = require('./learned.action')
const words = require('./words.action')
const remember = require('./remember.action')
const random = require('./random.action')

module.exports = {
    learned,
    words,
    remember,
    random
}